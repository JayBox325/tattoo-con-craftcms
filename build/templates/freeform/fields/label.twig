<div>
    <label for="{{ for }}" class="font-condensed uppercase text-3xl text-[var(--primary)] leading-none">{{ label }}</label>
    {% if instructions %}
        <p class="leading-tight mb-2 text-sm">{{ instructions }}</p>
    {% endif %}
</div>