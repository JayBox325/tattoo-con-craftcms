<div class="col-span-1 relative h-full">
    <div class="flex flex-col justify-between h-full p-6 sm:p-8 md:p-10 lg:p-12 xl:p-16 2xl:p-18">
        <div class="">
            <h3 class="title-4">{{ item.title }}</h3>
            <div class="mt-6">
                {{ item.introductionText }}
            </div>
        </div>
        <div class="pt-12 xl:pt-20">
            <a href="{{ item.url }}" class="btn before:absolute before:top-0 before:left-0 before:w-full before:h-full before:bg-transparent" aria-label="Read the {{ item.title }} article">Read more</a>
        </div>
    </div>
</div>